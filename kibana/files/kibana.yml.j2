server.name: "kibana"
server.host: "0.0.0.0"
elasticsearch.hosts: [
  {% for host in groups['elasticsearch_servers'] -%}
  "http://{{ hostvars[host].ansible_host }}:{{ es_http_port }}"
  {% if not loop.last %},{% endif %}
  {%- endfor %}
]
